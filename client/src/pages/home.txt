import React from 'react'
import { useTypewriter } from 'react-simple-typewriter'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faGithub, faLinkedin } from '@fortawesome/free-brands-svg-icons';

const externalLinks = [
    {
        icon: faGithub,
        link: ''
    },
    {
        icon: faLinkedin,
        link: ''
    },
]


const Home: React.FC = () => {
    const [text] = useTypewriter({
        words: ['Game', 'Software'],
        loop: true,
        deleteSpeed: 70,
        typeSpeed: 90,
    });

    return (
        <>
            <div className="container mx-32 flex flex-col items-start justify-center min-h-screen">
                <p className='text-primary text-base font-serif'> <span> {text} </span> Developer</p>
                <h1 className="text-5xl text-primary font-sans font-semibold">
                    Hello I'm
                </h1>
                <h1 className="text-5xl text-secondary font-sans font-semibold">
                    James Anderson
                </h1>
                <p className="text-primary font-serif">
                    constantly learning, developing and refining my skills.
                </p>
                <div className='flex flex-row justify-between items-center gap-4'>
                    <button className='border-secondary border-2 px-4 py-2 rounded-full'>
                        <p className='text-secondary'> Download CV </p>
                    </button>
                    <div className="pl-44">
                        {externalLinks.map((link, index) => (
                            <a href={link.link} key={index}>
                                <FontAwesomeIcon icon={link.icon} size='lg' className='text-secondary text-xl px-2'/>
                            </a>
                        ))}
                    </div>

                </div>
            </div>
        </>
    )
}

export default Home
